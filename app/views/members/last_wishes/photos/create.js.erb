<% if @error.present? %>
  $.growl.error({ title: 'Error!', message: "<%= j @error %>" });
<% else %>
  $.growl.notice({ title: 'Success!', message: "Photo has been added" });
  $('#modal').modal('hide');
  $('#modal').on('hidden.bs.modal', function (e) {
    $('#photos_panel .panel-body').html('<%= j render :partial =>"index" %>');
  })
<% end %>