<% if @error.present? %>
  $.growl.error({ title: 'Error!', message: "<%= j @error %>" });
<% else %>
  $.growl.notice({ title: 'Success!', message: "<%= @possession.what %> has been updated" });
  $('#modal').modal('hide');
  $('#modal').on('hidden.bs.modal', function (e) {
    $('#possessions_panel .panel-body').html('<%= j render :partial =>"index" %>');
  })
<% end %>